# NOTE: dependencies are now defined in pyproject.toml (managed with uv).
# This file is kept only for compatibility with existing experiment protocol
# setups that run `pip install -r requirements.txt`, and may be removed in the future.
pyside6
qtpy
attrs
numpy
pandas
xlrd
pyttsx3 # for text-to-speech
pypiwin32 # required by pyttsx3 on windows
pywinauto
pyqtgraph
zmq
pyperclip
psutil
command_runner
telnetlib3 ; python_version >= "3.13"  # for vlc control (telnetlib removed from stdlib in 3.13)